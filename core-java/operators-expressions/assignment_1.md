Java Operators & Expressions — MCQ Assignment
1. Which of the following is an arithmetic operator in Java?

    A) &&  
    B) +=  
    C) %  
    D) ==  

2. What is the output of:
    ```java
    int a = 10, b = 3;
    System.out.println(a / b);
    ```

    A) 3.3333  
    B) 3  
    C) 4  
    D) Error  

3. The modulus operator % gives:

    A) The quotient  
    B) The remainder  
    C) The average  
    D) The sum  

4. What is the output?
    ```java
    int a = 10, b = 5;
    System.out.println(a % b);
    ```

    A) 0  
    B) 2  
    C) 5  
    D) Error  

5. Which of the following is not a relational operator?

    A) !=  
    B) >=  
    C) ==  
    D) =  

6. What will 5 > 2 && 10 < 20 evaluate to?

    A) true  
    B) false  
    C) 1  
    D) 0  

7. What is the output?
    ```java
    int x = 10;
    System.out.println(x == 10);
    ```

    A) 10  
    B) true  
    C) false  
    D) Compile error  

8. Which of the following is a logical operator?

    A) ||  
    B) >  
    C) =  
    D) +=  

9. Which operator is used for assignment?

    A) ==  
    B) =  
    C) !=  
    D) :=  

10. What is the output?
    ```java
    int x = 5;
    x += 3;
    System.out.println(x);
    ```

    A) 5  
    B) 8  
    C) 3  
    D) Error  

11. What is the output of:
    ```java
    int a = 5;
    a *= 2 + 3;
    System.out.println(a);
    ```

    A) 10  
    B) 25  
    C) 15  
    D) 8  

12. What does ++x mean?

    A) Increment x after using it  
    B) Increment x before using it  
    C) Decrement x before using it  
    D) None  

13. What does x-- mean?

    A) Decrease x before using it  
    B) Decrease x after using it  
    C) Increase x before using it  
    D) None  

14. What is the output?
    ```java
    int x = 5;
    System.out.println(++x + x++);
    ```

    A) 10  
    B) 11  
    C) 12  
    D) 9   

15. What will be printed?
    ```java
    boolean b = true;
    System.out.println(!b);
    ```

    A) true  
    B) false  
    C) 0  
    D) 1  

16. The ternary operator uses:

    A) One operand  
    B) Two operands  
    C) Three operands  
    D) Four operands  

17. What is the correct syntax of the ternary operator?

    A) (condition) ? (value_if_true) : (value_if_false)  
    B) condition ? if_true : else_false;  
    C) (condition) : ? (true) (false)  
    D) if ? then : else  

18. What will the following code print?
    ```java
    int a = 10, b = 20;
    int max = (a > b) ? a : b;
    System.out.println(max);
    ```

    A) 10  
    B) 20  
    C) true  
    D) false  

19. What is the output?
    ```java
    int x = 10, y = 20;
    System.out.println(x > y ? x : y);
    ```

    A) 10  
    B) 20  
    C) 30  
    D) true  

20. Which of these has the highest precedence?

    A) *  
    B) +  
    C) =  
    D) &&  

21. What is the result of:
    ```java
    int x = 2 + 3 * 4;
    System.out.println(x);
    ```

    A) 20  
    B) 14  
    C) 24  
    D) 10  

22. Which of these operators has the lowest precedence?

    A) *  
    B) +  
    C) =  
    D) >  

23. Which expression is evaluated first in Java?

    A) Parentheses ()  
    B) Multiplication *  
    C) Division /  
    D) Addition +   

24. What is the output?
    ```java
    int a = 5, b = 10, c = 15;
    int result = a + b * c;
    System.out.println(result);
    ```

    A) 75  
    B) 225  
    C) 150  
    D) 25  

25. Which of the following has right-to-left associativity?

    A) +  
    B) *  
    C) =  
    D) /  

26. What is the output?
    ```java
    int a = 10;
    System.out.println(a++ + ++a);
    ```

    A) 21  
    B) 22  
    C) 20  
    D) 23  

27. What will be printed?
    ```java
    int x = 5;
    int y = 10;
    System.out.println(!(x > y));
    ```

    A) true  
    B) false  
    C) 1  
    D) 0  

28. What does this expression mean?
    ```java
    x = y = z = 5;
    ```

    A) Error  
    B) Assigns 5 to z only  
    C) Assigns 5 to x, y, and z  
    D) None  

29. What is the output?
    ```java
    int a = 10, b = 20, c = 5;
    System.out.println(a > b && b > c);
    ```

    A) true  
    B) false  
    C) 1  
    D) 0  

30. Which operator is evaluated last according to precedence rules?

    A) *  
    B) /  
    C) +  
    D) =  

<!-- 
## Answer Key

| Q  | Ans | Q  | Ans | Q  | Ans | Q  | Ans |
|----|-----|----|-----|----|-----|----|-----|
| 1  | C   | 2  | B   | 3  | B   | 4  | A   |
| 5  | D   | 6  | A   | 7  | B   | 8  | A   |
| 9  | B   | 10 | B   | 11 | B   | 12 | B   |
| 13 | B   | 14 | B   | 15 | B   | 16 | C   |
| 17 | A   | 18 | B   | 19 | B   | 20 | A   |
| 21 | B   | 22 | C   | 23 | A   | 24 | A   |
| 25 | C   | 26 | A   | 27 | A   | 28 | C   |
| 29 | B   | 30 | D   |    |     |    |     | -->
